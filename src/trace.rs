use crate::cpu::AddressingMode;
use crate::cpu::Mem;
use crate::cpu::CPU;
use std::collections::HashMap;

// This will print out the current status of the CPU in the format of
pub fn trace(cpu: &CPU) -> String {}
